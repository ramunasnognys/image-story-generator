import { ImageProcessingApp } from "@/components/image-processing-app"

/**
 * Renders the main page component of the image processing application
 * @returns {JSX.Element} The ImageProcessingApp component
 */
export default function Page() {
  return <ImageProcessingApp />
}